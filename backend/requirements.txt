# ==============================================================================
# TrAcEs — Trilha de Acompanhamento Estudantil
# Dependências do Projeto Backend Python (Clean Architecture & API RESTful)
# Compatibilidade: Python 3.10, 3.11, 3.12+
# Instalação: pip install -r requirements.txt
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. Framework e Execução (Runtime)
# O backend foi projetado sob os princípios de Clean Architecture e Zero Heavy Dependencies,
# utilizando os módulos nativos da biblioteca padrão do Python:
#  - http.server, urllib.request, urllib.parse (Servidor REST nativo de alta performance)
#  - sqlite3 (Banco de Dados relacional embarcado SQLite 3)
#  - json, dataclasses, enum, datetime, calendar, typing, pathlib (Modelagem de Domínio)
# ------------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# 2. Testes Automatizados e Qualidade de Software
# ------------------------------------------------------------------------------
pytest>=8.0.0
pluggy>=1.4.0
iniconfig>=2.0.0
packaging>=23.0
colorama>=0.4.6
Pygments>=2.17.0
